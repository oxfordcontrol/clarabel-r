# A persistent Clarabel solver that can be reused across R calls.

A persistent Clarabel solver that can be reused across R calls.

## Usage

``` r
ClarabelSolver
```

## Format

An object of class `clarabel::ClarabelSolver__bundle` (inherits from
`savvy_clarabel__sealed`) of length 1.
